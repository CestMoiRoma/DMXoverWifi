# Desktop-side test tooling. None of this ships to the board.
pytest>=8.0

# Drives a real Chromium against the web UI. Without it the browser tests skip
# and the rest of the suite still runs.
playwright>=1.40

# What tools/ imports for the serial paths. Present here so the suite can import
# those scripts and test the parts that do not need a board attached.
pyserial>=3.5
